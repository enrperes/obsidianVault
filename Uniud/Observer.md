```java
//NewsAgency 
public class NewsChannel implements Observer{
	String name = "";
	NewsPaperChannel(String name){this.name = name;}
	@Override
	public void update(Object data){
		System.out.format
	}
	
}
```

```java
// ClientObserver
ansa.addObserver(new NewsChannel(name:"rai2"));
ansa.addObserver(new NessPaperChannel(name: "Corriere"));
```